name: task-view
version: 0.1.0-task-view-prep
description: Browser viewer + editor for Knowledge Hub-style workflow ledgers (task-list / roadmap / backlog JSON). Per-record markdown mirrors + patch server + CLI + Claude plugin.
author: Knowledge Hub team
license: MIT/Apache2.0

includes:
  - apps/server/**/*
