---
type: task
id: "16"
title: ast-dataflow handover review + worktree closure
status: done
priority: must
effort_estimate: ~30-45min (read + verify + worktree teardown)
owner: Engineering
updated: 2026-05-18T21:00:00.000Z
session_refs: [kh-prod-readiness-S51, kh-prod-readiness-S52]
commit_refs: []
dependencies: []
cross_doc_links: 
  - path: docs/specs/ast-dataflow-tool/HANDOVER.md
    anchor: null
    raw: ast-dataflow handover doc
  - path: docs/runbooks/ast-dataflow-merge-S11.md
    anchor: null
    raw: Cross-track merge runbook
priority_note: Should land early in S52 — fresh context for any AST-related follow-on work.
status_note: Closed kh-prod-readiness-S52 WP0. 27 (OQ-R9 promotion) + ID-17 (RSS auth design) added to backlog as side-effect of §5/§6 cross-check.
---

# ID-16: ast-dataflow handover review + worktree closure

The ast-dataflow track merged its in-flight work to production-readiness during S51 (R-WP-S11-D, merge commit 03392948 + ast-track commits including ffc46cf7). A handover document was authored at `docs/specs/ast-dataflow-tool/HANDOVER.md` (154 lines, commit 16b6a5bb) covering: cross-track merge mechanics, the nine-session arc (kh-ast-S1 → S11), OPS-T1 codemod specs landed, six AST-prefixed backlog items now on production-readiness (since renumbered to ID-N via Task ID-15 migration), three S11 gotchas, carried-forward Liam ratifications, and read-first order for follow-on work.

This Task reviews the handover at session start, validates the 6 follow-on backlog items are correctly captured + sequenced, confirms the ast-dataflow worktree at `/Users/liamj/Documents/development/knowledge-hub-ast-dataflow` can be closed, and runs the worktree teardown.

## Acceptance criteria

- HANDOVER.md read in full; key points (TL;DR + section 2 merge contents + section 5 follow-on items + section 6 read-first order) captured into S52+ working context.
- 6 ast-dataflow follow-on backlog items verified present on production-readiness (post Task ID-15 migration, these will be renumbered from AST-S{N}-O{M} to ID-N format).
- Three S11 gotchas (worktree-isolation CWD drift, freshness-guard merge edge case, concurrent-dev reconciliation pattern) cross-checked against CLAUDE.md — added if not present.
- Worktree teardown: `git worktree remove /Users/liamj/Documents/development/knowledge-hub-ast-dataflow` (with `--force` only if Liam confirms no uncommitted work). Branch `ast-dataflow-tooling` deleted only after confirmation (post-merge tip SHA preserved in HANDOVER.md table for git-archaeology recovery).
- `git worktree list` post-teardown shows three top-level worktrees: main, production-readiness, kh-knowledge-platform.

## Dependencies

None (independent of in-flight WPs); ideally lands early in S52 so context is fresh before any AST-related work picks up.

## Subtasks

### ID-16.1: Read HANDOVER.md + cross-check follow-on backlog items

- **Status:** done
- **Dependencies:** _none_
- **Updated:** _unset_

Read the full handover doc + verify the 6 ast-dataflow follow-on items are correctly captured in product-backlog.json.

**Test strategy:** All 6 follow-on items located in product-backlog.json; gotchas cross-checked against CLAUDE.md; reading captured in session-state diary note.

**Details:**

File: docs/specs/ast-dataflow-tool/HANDOVER.md (154 lines).

Read in full. Capture into S52 working context:
- TL;DR (§1): 11 queries + 2 ESLint rules + structured error contract + CLI; 135 ast-exclusive files + 21 overlap + OPS-T1 codemod specs.
- Section 2 (merge contents): 167 commits, 6 file categories, two non-mechanical decisions (terminology unification, forward-discipline backlog rule).
- Section 5 (follow-on items): 6 backlog items with sequencing.
- Section 6 (gotchas): three S11 gotchas — verify against CLAUDE.md, add any missing.
- Section 7 (read-first order): the prescribed reading sequence for picking up 25 (OPS-T1 codemod impl).

Verify: grep `product-backlog.json` for `25`, `26`, `23`, `24` (the legacy IDs — these will get renumbered when Task ID-15 lands).

<info added on 2026-05-18T21:00:00.000Z>
HANDOVER.md read in full (154 lines). Captured into S52 working context: TL;DR (§1) — 11 queries + 2 ESLint rules + structured error contract + CLI; 135 ast-exclusive + 21 overlap + OPS-T1 codemod specs. §2 — 167 commits merged, two non-mechanical decisions ratified (terminology unification: task ID-N wins; forward-discipline backlog rule: archive don't retain). §5 — six follow-on backlog items verified present in product-backlog.json at lines 497/509/521/533/545/557 (AST-S3-O1, AST-S3-O2, 23, 24, 25, 26). §6 RSS-route finding (NAKED_NO_AUTH on `app/api/feeds/[workspaceId]/rss/route.ts` + `/filtered/route.ts`): confirmed intentional via service-client + proxy.ts:111 isApiRoute exemption; Liam decision = defer to backlog as new ID-17 for proper RSS auth design. §7 carried-forward ratifications captured; OQ-R9 (opaque-Json RPC migration ~7h) NOT numbered in backlog despite ROADMAP.md "Carried forward" reference — Liam decision = promote as 27 (legacy format, slated for ID-15 renumber).
</info added on 2026-05-18T21:00:00.000Z>

### ID-16.2: Add missing S11 gotchas to CLAUDE.md if not present

- **Status:** done
- **Dependencies:** ID-16.1
- **Updated:** _unset_

Cross-check the three S11 gotchas from HANDOVER.md against CLAUDE.md and add any missing entries.

**Test strategy:** CLAUDE.md contains all three gotchas with reference to HANDOVER.md §6.

**Details:**

Three S11 gotchas to check:
1. Worktree-isolation CWD drift (likely already in CLAUDE.md — verify wording).
2. Freshness-guard merge edge case (combined-diff format does not register single-parent last_updated additions on merge commits — this is the bug behind kh-ast-S11 R-WP-S11-D freshness-guard bump 744d9ef1).
3. Concurrent-dev reconciliation pattern (when two worktrees touch the same file).

If any are missing, add to CLAUDE.md "Gotchas" section. Reference HANDOVER.md §6 for exact wording.

<info added on 2026-05-18T21:00:00.000Z>
Cross-check confirmed: NONE of the three S11 gotchas were previously in CLAUDE.md. CLAUDE.md HAD related but distinct gotchas ("Bash CWD drifts into worktree dirs after Read" + "Sub-agent instructions must always use relative paths") covering different failure modes. Added all three S11 gotchas to CLAUDE.md "Worktree isolation rules" sub-section, with specific commit references per HANDOVER.md §8:
1. `isolation: "worktree"` sub-agents can still commit to parent worktree (cites commits 6dbf9da2 + af9fe05c from kh-ast-S11 R-WP-S11-C as evidence; mitigation = prefix `cd $(git rev-parse --show-toplevel) &&` after Read + verify pwd).
2. Reference-doc freshness guard fails on merge commits (combined-diff format; workaround = follow-up single-parent commit per `744d9ef1` precedent; or `[skip-doc-freshness-guard]` body tag).
3. Concurrent commits on production-readiness during cross-track merge (cites b6f7d55f + 80944a09 + 831d9e74; pattern = fresh worktree off production-readiness + reconcile-merge pattern per `docs/runbooks/ast-dataflow-merge-S11.md`).
</info added on 2026-05-18T21:00:00.000Z>

### ID-16.3: ast-dataflow worktree teardown

- **Status:** done
- **Dependencies:** ID-16.2
- **Updated:** _unset_

Remove the ast-dataflow worktree post-handover. Requires Liam confirmation that no uncommitted work remains.

**Test strategy:** `git worktree list` post-teardown shows three top-level worktrees only. Branch `ast-dataflow-tooling` deletion is gated on Liam confirmation.

**Details:**

Pre-flight: `cd /Users/liamj/Documents/development/knowledge-hub-ast-dataflow && git status` — must be clean. If unstaged or untracked files exist, surface to Liam.

Teardown:
```
cd /Users/liamj/Documents/development/knowledge-hub-production-readiness
git worktree remove /Users/liamj/Documents/development/knowledge-hub-ast-dataflow
```

Branch `ast-dataflow-tooling` deletion (separate, gated on Liam confirmation): `git branch -D ast-dataflow-tooling` ONLY after the worktree is removed AND Liam confirms branch isn't needed for further git-archaeology (the merge commit 5d28cd08 + the tip SHA `ffc46cf7` documented in HANDOVER.md §3 preserve the history regardless).

Final check: `git worktree list` must show only main + production-readiness + kh-knowledge-platform.

<info added on 2026-05-18T21:00:00.000Z>
Pre-flight: ast-dataflow worktree clean (`git status` returned 'nothing to commit, working tree clean'); on branch `ast-dataflow-tooling`; tip SHA `ffc46cf7` matched HANDOVER.md §12 reference. Liam confirmed proceed-with-remove.

Teardown executed in two steps (sandbox required `dangerouslyDisableSandbox: true` per CLAUDE.md cocoindex/worktree gotcha pattern):
1. `git worktree remove` initially errored sandbox-side with 'Operation not permitted' but DID clean the .git/worktrees pointer (git's atomic metadata cleanup runs before fs delete). Re-running gave 'not a working tree' confirming git-side already done.
2. `rm -rf /Users/liamj/Documents/development/knowledge-hub-ast-dataflow` cleared the orphan directory.

Final state verified: `git worktree list` shows three top-level worktrees only (main on content-items-investigation, production-readiness, kh-knowledge-platform). Branch `ast-dataflow-tooling` PRESERVED per HANDOVER.md §12 (`do NOT delete per S11 close-out brief`); tip `ffc46cf7` remains on origin.
</info added on 2026-05-18T21:00:00.000Z>
