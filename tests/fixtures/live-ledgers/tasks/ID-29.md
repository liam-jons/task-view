---
type: task
id: "29"
title: TanStack workspace-types migration (Option C hybrid metadata) — backlog ID-21 promotion
status: done
priority: low
effort_estimate: ~3-4h impl (per backlog ID-21 estimate, carried over; PLAN.md 29.4 confirms ~4h across 4 Subtasks 29.5..29.8)
owner: Engineering
updated: 2026-05-21T18:45:00.000Z
session_refs: [main-track-S251, main-track-S252]
commit_refs: [8581bf4f]
dependencies: []
cross_doc_links: 
  - path: docs/specs/tanstack-workspace-types/TECH.md
    anchor: null
    raw: Ratified TECH.md (S251 commit 8581bf4f) — P-1..P-5 + Q-1..Q-5 + Option C hybrid metadata
  - path: lib/workspace-types.ts
    anchor: null
    raw: Source file — static WORKSPACE_TYPE_REGISTRY to be replaced
  - path: lib/validation/schemas.ts
    anchor: "#L535"
    raw: Sync Zod constraint — APPLICATION_TYPE_KEYS hardcoded tuple preserved
  - path: docs/specs/canonical-pipeline-implementation-plan/PRODUCT.md
    anchor: "#q-oqr1-13"
    raw: Q-OQR1-13 v1.0/v1.1 cross-ref — admin UI metadata location ratification
priority_note: "Carried over from backlog ID-21 (`priority: low`)."
status_note: Promoted retroactively from backlog ID-21 during main-track-S252. Subtasks 29.1/29.2/29.3 retrofitted to reflect as-is state. 29.4 PLAN.md authored S252 WP5 (worktree branch `worktree-agent-aae48793fa368be7a`, fresh task-planner instance per Q-PLANNER-2); decomposed into 4 implementation Subtasks 29.5..29.8 with strictly linear sibling-only deps. Awaiting Liam ratification gate before Orchestrator dispatches 29.5 to task-executor.
---

# ID-29: TanStack workspace-types migration (Option C hybrid metadata) — backlog ID-21 promotion

Migrate `lib/workspace-types.ts` static `WORKSPACE_TYPE_REGISTRY` to TanStack Query against the `application_types` table, using the Option C hybrid metadata pattern ratified S251 (admin-editable copy in `application_types` DB columns; dev-editable routing/features in static client map in `hooks/use-application-types.ts`).

**Promoted retroactively from backlog ID-21 during main-track-S252.** Spec chain partially complete at promotion: RESEARCH (Subtask 29.1) captured inline via backlog notes (no formal RESEARCH.md authored); PRODUCT (Subtask 29.2) intentionally skipped per S251 ratification (refactor, no behavioural change); TECH (Subtask 29.3) authored + ratified S251 at `docs/specs/tanstack-workspace-types/TECH.md` (commit 8581bf4f); PLAN (Subtask 29.4) is the next step — WP5 dispatch this session.

## Acceptance criteria (carried from S251 ratified TECH.md)

- DDL migration: ADD COLUMN `label_plural` + `description` on `application_types` + backfill 6 seed rows.
- `useApplicationTypes()` TanStack hook + `/api/application-types` route created.
- 3 UI consumer migration: `app/workspaces/workspaces-content.tsx`, `components/workspace/workspace-card.tsx`, `components/workspace/workspace-create-dialog.tsx`.
- Delete static `WORKSPACE_TYPE_REGISTRY` + `registerType()` + `getWorkspaceType()` + `formatTypeCount()`; update `__tests__/lib/workspace-types.test.ts`; add `__tests__/hooks/use-application-types.test.ts`.
- `APPLICATION_TYPE_KEYS` hardcoded tuple preserved for sync Zod enum (`lib/validation/schemas.ts:535`) — async TanStack cannot back module-load Zod construction.

## Dependencies

None blocking. Spec chain ratified S251. Implementation slices decomposed by Subtask 29.4 PLAN this session.

<info added on 2026-05-21T17:55:00.000Z>
Promoted retroactively from backlog id=21 during main-track-S252. Rationale: TECH.md ratified S251 commit 8581bf4f without prior Task-list tracking (new dev workflow not fully in place at S251 close — Promote convention codified S60 ID-15.10 Phase E). Retroactive Subtasks reflect as-is spec chain state: 29.1 RESEARCH done (backlog notes serve), 29.2 PRODUCT deferred (skipped per S251 ratification), 29.3 TECH done (commit 8551bf4f preserved in backlog notes), 29.4 PLAN.md to be authored WP5 this session. Backlog ID-21 entry removed (atomic Promote semantics — no idempotency violation since this is a one-time retroactive transition).
</info added on 2026-05-21T17:55:00.000Z>

<info added on 2026-05-22T13:30:00.000Z>
**Task-level code-simplification pass (end-of-Task) — S256.**

Applied `code-simplification` skill across the ID-29 implementation surface after all impl Subtasks (29.5..29.8) shipped. Addresses 7 curated nits from S254 W2 + S255 W1 Checker verdicts + general DRY-up. NOT an impl Subtask — Task ID-29 status flip is the Orchestrator's call post-Checker quality review.

**Nits addressed:**
1. S254 W2 #1 — `useLauncherTypes` exclusion test coverage: added test case (d2) asserting all 6 current seeds pass the filter (`route !== null || !available`); documents dead-exclusion property so future seed additions can't silently slip launcher inclusion.
2. S254 W2 #2 — mock-strategy clarification: added explicit "Mock strategy" JSDoc section to `__tests__/hooks/workspaces/use-application-types.test.ts` documenting the `vi.stubGlobal('fetch', ...)` pattern + rationale (hook = HTTP boundary; exercises real `useQuery` machinery).
3. S254 W2 #3 — `'use client'` directive placement: moved from L28 (after JSDoc) to L1 in `hooks/workspaces/use-application-types.ts` matching project convention (verified across `hooks/use-tags-data.ts`, `hooks/use-notifications.ts`, etc.); JSDoc now follows the directive.
4. S255 W1 #1 — `SEED_ROWS_SNAKE` + `createWrapper()` duplication: extracted shared helper at `__tests__/helpers/workspace-type-fixtures.ts` exporting `SEED_APPLICATION_TYPE_ROWS` + `stubApplicationTypesFetch()`. Migrated 5 test files to consume the helper + project-wide `createQueryWrapper()` from `__tests__/helpers/query-wrapper.tsx` (already-established convention). Net deletion: ~360 lines of identical boilerplate across the 5 files.
5. S255 W1 #2 — `waitFor` timing sentinel: replaced `expect(global.fetch).toHaveBeenCalled()` in `workspace-card.test.tsx` with scoped `expect(mockFetch).toHaveBeenCalled()` (captured from `stubApplicationTypesFetch()` return). Marginal but cleaner — references test's own scope.
6. General DRY-up — 3 hooks consolidated: `useApplicationTypes()`/`useWorkspaceType()`/`useLauncherTypes()` previously triple-duplicated the `{ queryKey, queryFn, select, staleTime }` block. Extracted private `useApplicationTypesQuery<T>(select)` helper that hosts the shared config; each public hook is now a 1-line wrapper passing the `select` callback. The shared `queryKey` was already correct — TanStack Query dedupes the fetch automatically.
7. General dead-export cleanup — 3 unused exports dropped:
   - `EMPTY_TYPES` (hook module) — module-level stable-empty-array constant + re-export; zero consumers, knip-flagged.
   - `ApplicationTypeRow` (hook module) — exported interface only used by local `WorkspaceTypeConfig extends`; dropped `export` keyword (interface itself retained for the `extends` clause).
   - `APPLICATION_TYPE_KEYS` (lib/workspace-types.ts) — internal sync constant only used by local `getValidTypeValues()`; dropped `export` keyword. Comment block updated to reflect module-internal status.

**Production code changes (defensive):**
- `components/workspace/workspace-card.tsx` L51: dropped `= null` default on `useWorkspaceType()` destructure (S255 W1 close `= null` nit fix per brief). `null` vs `undefined` JSX guards behave identically against `typeConfig &&` / `typeConfig?.` patterns; the change aligns with `workspace-create-dialog.tsx:56` convention.

**Files touched (8 modified + 1 new):**
- New: `__tests__/helpers/workspace-type-fixtures.ts` (+123 lines, shared SEED + fetch stub)
- Modified: `__tests__/hooks/workspaces/use-application-types.test.ts`, `__tests__/components/workspace-card.test.tsx`, `__tests__/components/workspace-create-dialog.test.tsx`, `__tests__/app/workspaces/workspaces-content.test.tsx`, `__tests__/app/workspaces/workspaces-launcher.test.tsx`, `hooks/workspaces/use-application-types.ts`, `components/workspace/workspace-card.tsx`, `lib/workspace-types.ts`

**Net diff:** -529 +162 = -367 lines from modified files + 123-line new helper = ~-244 lines net (significant simplification, matching `code-simplification` skill SIMPLIFYING-diff criterion).

**Verification:**
- All 50 tests across 7 affected files PASS (was 45 at S255 W2 close; +5 includes (d2) exclusion test + sanity-check + earlier additions).
- Broader workspaces regression (223 files / 2917 tests) PASS.
- `bun lint` CLEAN.
- `bun run knip` net −2 unused exports + −1 unused exported type vs baseline; ZERO new orphans introduced.
- `bun run build` pre-existing baseline failure (worktree-specific `.bin` ENOENT) — proven by stash-and-rerun; OUT OF SCOPE.
- `bunx tsc --noEmit` only pre-existing `database.types.ts:4539` errors (S255 W1 carry); ZERO new TS errors in touched surface.
- `__tests__/validation/` 3 pre-existing failures (1 doc-freshness `taxonomy-change-runbook.md`, 2 inline `.safeParse()` on `app/api/internal/pipeline-runs/record/route.ts` — ID-28 footprint, OUT OF SCOPE per brief).

**Out-of-scope observations (for Curator triage):**
- `app/api/internal/pipeline-runs/record/route.ts` uses inline `.safeParse()` instead of `parseBody/parseSearchParams` per project validation pattern. Pre-existing on ID-28 footprint (28.11 webhook + recordPipelineRun pipeline). Not introduced this pass.
- `docs/operations/taxonomy-change-runbook.md` missing — doc-freshness drift. Pre-existing baseline.
- `database.types.ts:4539` TS syntax error — pre-existing baseline carried since pre-S255.

**No spec changes** — PRODUCT.md/TECH.md/PLAN.md unchanged. Sync constraint anchor `lib/validation/schemas.ts` unchanged. No new features. No DDL changes.

**Task ID-29 status:** UNTOUCHED — remains `in_progress`. Orchestrator's call to promote post-Checker quality-review pass on this simplification commit.
</info added on 2026-05-22T13:30:00.000Z>

## Subtasks

### ID-29.1: RESEARCH — workspace-types async-sync seam + Option C decision

- **Status:** done
- **Dependencies:** _none_
- **Updated:** _unset_

Backlog ID-21 notes captured the research inline: async-sync seam at `lib/validation/schemas.ts:535` (Zod enum at module-load via `z.enum(getValidTypeValues())` — sync required), 3 UI consumers identified, Option C hybrid metadata pattern ratified S251.

**Test strategy:** Research recorded via backlog ID-21 notes (preserved in promotion journal block). TECH.md §1-§3 absorbs the findings.

**Details:**

Retroactive RESEARCH — no formal RESEARCH.md authored. Source-of-truth: backlog ID-21 `notes` field at S251 close (preserved in promotion journal block on this Task) + `lib/workspace-types.ts` header comment block + PLAN.md §4.4 T4 acceptance criterion.

Findings carried into Subtask 29.3 TECH.md ratification:
- Constraint: `lib/validation/schemas.ts:535` constructs Zod enum at module-load — cannot be async TanStack useQuery.
- Scope: keep `APPLICATION_TYPE_KEYS` hardcoded tuple for sync Zod; migrate 3 UI consumers to `useApplicationTypes()` hook; delete static `WORKSPACE_TYPE_REGISTRY`.
- Open question (resolved at TECH ratification S251 → Option C hybrid): where does UI metadata live? Answer: split — admin-editable (`label`, `label_plural`, `description`, `default_icon`, `default_colour`) in DB columns, dev-editable routing/features in static client map.

<info added on 2026-05-21T17:55:00.000Z>
Retroactive close: backlog ID-21 notes field served as the research record. Findings are absorbed into TECH.md §1-§3 (Option C decision).
</info added on 2026-05-21T17:55:00.000Z>

### ID-29.2: PRODUCT — Intentionally skipped (refactor, no behavioural change)

- **Status:** deferred
- **Dependencies:** _none_
- **Updated:** _unset_

PRODUCT.md (Behavior invariants) intentionally skipped per S251 ratification — this is a refactor with no behavioural change. TECH.md (29.3) covers the refactor shape directly.

**Details:**

Per S251 ratification recorded in `docs/specs/tanstack-workspace-types/TECH.md` header: this is a refactor with no behavioural change — `WORKSPACE_TYPE_REGISTRY` static map → `application_types` DB-backed hook is a data-source swap, not a feature behaviour change. PRODUCT.md (Behavior invariants) is not load-bearing. TECH.md §1 (Proposed changes) and §2 (Acceptance) cover the refactor invariants directly.

Subtask status `deferred` chosen because SubtaskStatus schema (`task-list-schema.ts:44-48`) excludes `cancelled` at Subtask level — only Task-level can be `cancelled`. Semantic equivalent here is 'deliberately not authored' rather than 'postponed' — but `deferred` is the closest schema-permitted status.

<info added on 2026-05-21T17:55:00.000Z>
Retroactive deferred-status record: S251 ratification was Liam's call (refactor scope). No PRODUCT.md exists; TECH.md §1 invariants serve as the behaviour contract for the refactor.
</info added on 2026-05-21T17:55:00.000Z>

### ID-29.3: TECH — Path (c) Option C hybrid metadata spec (ratified S251)

- **Status:** done
- **Dependencies:** ID-29.1
- **Updated:** _unset_

TECH.md authored S251 commit 8581bf4f at `docs/specs/tanstack-workspace-types/TECH.md` (560 lines, P-1..P-5 Proposed changes + Q-1..Q-5 ratifications + Option C hybrid Q-OQR1-13 v1.1 cross-ref).

**Test strategy:** TECH.md present at `docs/specs/tanstack-workspace-types/TECH.md`. Ratified S251 commit 8581bf4f. 560 lines. P-1..P-5 invariants explicit.

**Details:**

Authored S251 WP3 by fresh task-planner (opus, worktree commit 246c4757, cherry-pick 79fc2e80). Ratified at S251 close with Q-1..Q-5 default ratifications + Q-OQR1-13 v1.0/v1.1 cross-ref absorbed into canonical `docs/specs/canonical-pipeline-implementation-plan/PRODUCT.md`.

P-N Proposed changes:
- P-1: Add `label_plural` + `description` columns to `application_types` table (admin-editable copy).
- P-2: Create `useApplicationTypes()` TanStack hook + `/api/application-types` route.
- P-3: Migrate 3 UI consumers (workspaces-content, workspace-card, workspace-create-dialog) to the hook.
- P-4: Delete static `WORKSPACE_TYPE_REGISTRY` + `registerType()` + `getWorkspaceType()` + `formatTypeCount()`.
- P-5: Update test fixtures + add hook tests.

Q-1..Q-5 ratifications + Option C metadata partition (admin-editable in DB, dev-editable in static client map) recorded in TECH.md §4-§5.

<info added on 2026-05-21T17:55:00.000Z>
Retroactive done-status close: TECH.md ratified S251 commit 8581bf4f. Spec at `docs/specs/tanstack-workspace-types/TECH.md`. Subtask 29.4 PLAN.md is the next step (WP5 this session).
</info added on 2026-05-21T17:55:00.000Z>

### ID-29.4: PLAN — Decompose TECH.md into TM-shape implementation Subtasks 29.5+

- **Status:** done
- **Dependencies:** ID-29.3
- **Updated:** 2026-05-21T18:45:00.000Z

Fresh task-planner invokes planning-and-task-breakdown skill against ratified TECH.md. Likely shape: DDL migration + hook+route + 3 UI consumer migration + delete static + tests.

**Test strategy:** PLAN.md present at `docs/specs/tanstack-workspace-types/PLAN.md`. Subtasks 29.5+ appended to task-list.json with populated `details` + `testStrategy`. Sibling-only deps (no cross-Task refs). Liam ratification gate cleared before implementation Subtask dispatch. `<info added on …>` journal block.

**Details:**

Fresh task-planner dispatch (opus, isolation:worktree) for WP5 this session.

Output: `docs/specs/tanstack-workspace-types/PLAN.md` + task-list.json Subtask append (29.5+).

Invokes `planning-and-task-breakdown` skill against `docs/specs/tanstack-workspace-types/TECH.md`. Decompose into TM-shape Subtask records with load-bearing `details` + one-line `testStrategy` + sibling-only dependencies. Expected Subtask shape (likely):
(1) DDL migration: ADD COLUMN `label_plural` + `description` on `application_types` + backfill 6 seed rows.
(2) `useApplicationTypes()` hook + `/api/application-types` route.
(3) 3 UI consumer migration: `app/workspaces/workspaces-content.tsx`, `components/workspace/workspace-card.tsx`, `components/workspace/workspace-create-dialog.tsx`.
(4) Delete static `WORKSPACE_TYPE_REGISTRY` + `registerType()` + `getWorkspaceType()` + `formatTypeCount()` + update `__tests__/lib/workspace-types.test.ts` + add `__tests__/hooks/use-application-types.test.ts`.

Sibling-only deps. If cross-Task deps surface (e.g., shared schema with T8), escalate to Task-split rather than bending the constraint.

<info added on 2026-05-21T19:45:00.000Z>
PLAN.md authored at `docs/specs/tanstack-workspace-types/PLAN.md` (fresh task-planner instance per Q-PLANNER-2, worktree branch `worktree-agent-aae48793fa368be7a`). Decomposed TECH.md P-1..P-5 + §6 migration into 4 sequential implementation Subtasks (29.5..29.8) per the strict dependency chain enforced by TECH.md §5 (migration→regen→hook→consumers→deletion).

Decomposition summary:
- **29.5** — DDL migration + backfill + regen DB types (~1h)
- **29.6** — `useApplicationTypes()` hook + `/api/application-types` route + queryKey + hook tests (~1.5h)
- **29.7** — 3 UI consumer migration to hook (~0.75h)
- **29.8** — Delete static `WORKSPACE_TYPE_REGISTRY` surface + align legacy tests (~0.75h)

Dependency graph: strictly linear `29.5 → 29.6 → 29.7 → 29.8`. No parallelism opportunity (TECH.md §5 explicit: reversing migration↔hook would ship NULL-filled rows). All sibling-only — zero cross-Task dependencies surfaced. 25-Subtask soft ceiling: 4/25 — comfortably within.

Total effort estimate: ~4h (matches Task ID-29 `effort_estimate: '~3-4h impl'` carried from backlog ID-21). Open questions: zero (all S251 Q-1..Q-5 already ratified).

Planning-layer risks surfaced beyond TECH.md §7: (R-6) stable empty-array discipline for hook return per CLAUDE.md, (R-7) sandbox-disable required for `supabase` CLI per CLAUDE.md, (R-8) `cat supabase/.temp/project-ref` discipline before each push per CLAUDE.md. All three baked into the relevant Subtask `details` field.

Awaiting Liam ratification gate before Orchestrator dispatches 29.5 to task-executor.
</info added on 2026-05-21T19:45:00.000Z>

<info added on 2026-05-21T18:45:00.000Z>
Liam ratification @ S252 Wave 1 close: PASS on PLAN.md decomposition. 4 implementation Subtasks 29.5..29.8 with strictly linear
dep graph (29.5 → 29.6 → 29.7 → 29.8), total effort ~4h, zero open questions (TECH.md Q-1..Q-5 already ratified S251).

Implementation Subtasks ready for impl dispatch in S253+ — no further ID-29 work this session.

Status: in_progress → done.
</info added on 2026-05-21T18:45:00.000Z>

### ID-29.5: DDL migration — add label_plural + description columns + backfill 6 application_types seed rows + regen DB types

- **Status:** done
- **Dependencies:** _none_
- **Updated:** _unset_

Author and apply the Option C hybrid metadata DDL migration: ADD COLUMN `label_plural` + `description` on `application_types`; backfill the 6 seed rows verbatim per TECH.md §6; regen `supabase/types/database.types.ts`.

**Test strategy:** Migration applies cleanly to both staging (`turayklvaunphgbgscat`) and prod (`rovrymhhffssilaftdwd`); post-apply `SELECT key, label, label_plural, description, default_icon, default_colour FROM application_types ORDER BY key` returns 6 rows with non-NULL `label_plural` + `description` + `default_icon` + `default_colour` on the 3 currently-rendered types (`procurement`, `intelligence`, `sales_proposal`); `supabase/types/database.types.ts` regenerated to include the 2 new column triples.

**Details:**

**TECH.md ref:** §6 (Migration plan) — full SQL template in TECH.md L412-483.

**Migration filename:** `supabase/migrations/<timestamp>_id_21_application_types_metadata_columns.sql` (use `/opt/homebrew/bin/supabase migration new id_21_application_types_metadata_columns` to scaffold).

**SQL shape (copy verbatim from TECH.md §6):**
- `SET search_path = public, extensions;` at the top (CLAUDE.md PG-function gotcha — though this migration adds no functions, set anyway for explicit-search-path discipline).
- `ALTER TABLE public.application_types ADD COLUMN label_plural text NULL, ADD COLUMN description text NULL;`
- Backfill UPDATEs for the 3 currently-rendered types (`procurement`, `intelligence`, `sales_proposal`) with verbatim values from the static `WORKSPACE_TYPE_REGISTRY` in `lib/workspace-types.ts` L81-136. Also backfills `default_icon` + `default_colour` for these 3 rows (currently NULL in DB).
- Default-backfill UPDATEs for the 3 non-rendered seed rows (`product_guide`, `competitor_research`, `training_onboarding`) — `label_plural = label || 's'`, `description = label`, NULL-coalesce guard.
- Post-check `DO $$` block asserts the 3 currently-rendered types have no NULLs in `label_plural`, `description`, `default_icon`, `default_colour`.

**No REVOKE block required** — this migration adds no `CREATE OR REPLACE FUNCTION public.*` (the anonymous `DO $$` block does not register as a callable function). CLAUDE.md S250 W1b anon-EXECUTE gotcha applies only to function creation.

**RLS / grants** — `application_types` table already has RLS enabled + `application_types_select_all FOR SELECT USING (true)` policy from T2 (migration `20260520120828` §1.1). New columns inherit the policy without modification.

**CLI apply discipline (CLAUDE.md gotchas — load-bearing):**
1. `cat supabase/.temp/project-ref` before each push (drift can silently push to wrong env).
2. Staging-apply first: `/opt/homebrew/bin/supabase db push --linked turayklvaunphgbgscat`.
3. Liam ratification gate.
4. Prod-apply: `/opt/homebrew/bin/supabase db push --linked rovrymhhffssilaftdwd`.
5. Regen types: `/opt/homebrew/bin/supabase gen types typescript --project-id rovrymhhffssilaftdwd --schema public > supabase/types/database.types.ts`.
6. Commit the regenerated `database.types.ts` in the same commit as the migration.

**Sandbox discipline:** ALL `supabase` CLI calls (migration new, db push, gen types) MUST use `dangerouslyDisableSandbox: true`. `POSTGRES_PASSWORD` must be set as a shell env var. Without sandbox-disable, the bun process hangs on HTTP 204 returns (CLAUDE.md gotcha).

**Post-apply verification (manual + scripted):**
- `SELECT key, label, label_plural, description, default_icon, default_colour FROM application_types ORDER BY key;` shows 6 rows.
- The 3 currently-rendered types (`procurement`, `intelligence`, `sales_proposal`) have non-NULL values for all 4 metadata fields.
- `supabase/types/database.types.ts` line ~17-52 `application_types` Row/Insert/Update now includes `label_plural: string | null` + `description: string | null`.

**Files touched:**
- `supabase/migrations/<timestamp>_id_21_application_types_metadata_columns.sql` (new)
- `supabase/types/database.types.ts` (regenerated; ~30-line diff on the application_types block)

**Verification gate after this Subtask:** staging + prod both clean; `application_types` row inspection shows 6 backfilled rows; database.types.ts regenerated and committed.

<info added on 2026-05-21T20:40:49.000Z>
**Shipped:** Migration `20260521203659_id_21_application_types_metadata_columns.sql` applied cleanly to both staging (turayklvaunphgbgscat) and prod (rovrymhhffssilaftdwd). Adds `label_plural text NULL` + `description text NULL` to `public.application_types`; backfills all 6 seed rows; post-check DO $$ block passed (0 NULL rows in rendered-type set). `supabase/types/database.types.ts` regenerated: `application_types` Row/Insert/Update now includes both new column triples.
**Commit:** 79ba756e (full: 79ba756e3a43887b6232f99cd9248121a2c8a1a8)
**Files touched:** supabase/migrations/20260521203659_id_21_application_types_metadata_columns.sql, supabase/migrations/20260521203414_t8_op_id_propagation.sql (parallel T8 ID-28.5 stub — needed for CLI migration history consistency), supabase/types/database.types.ts, docs/reference/task-list.json
**Acceptance:** Migration applied staging + prod ✓; post-apply SELECT returns 6 rows with procurement/intelligence/sales_proposal all non-NULL on 4 metadata fields ✓; database.types.ts includes `description: string | null` + `label_plural: string | null` on application_types Row/Insert/Update ✓
**Deviations:** (1) No `IF NOT EXISTS` guards on ADD COLUMN — TECH.md §6 verbatim does not include them (brief mentions idempotency discipline but TECH.md SQL is the authoritative source per brief). (2) `default_icon` for `intelligence` set to `'newspaper'` per TECH.md §6 — differs from `lib/workspace-types.ts` L130 which has `defaultIcon: 'globe'`; TECH.md verbatim takes precedence per brief. (3) Parallel T8 migration (20260521203414) was already applied to staging/prod by ID-28.5 executor; created local stub to repair CLI history; this stub will need to be reconciled at cherry-pick time (Orchestrator handles merge order per brief). (4) Supabase CLI appended update-notice text to types output — stripped before commit.
**Out-of-scope observations:** `lib/workspace-types.ts` `intelligence` registration uses `defaultIcon: 'globe'` but TECH.md §6 backfill uses `default_icon = 'newspaper'`. Checker should confirm which is canonical — the DB now has `newspaper`. This discrepancy should be flagged to the Orchestrator for Curator triage.
</info added on 2026-05-21T20:40:49.000Z>

### ID-29.6: Author useApplicationTypes() TanStack hook + /api/application-types route + queryKey namespace + hook tests

- **Status:** done
- **Dependencies:** ID-29.5
- **Updated:** _unset_

Create the `useApplicationTypes()` TanStack Query hook + accompanying GET-only authenticated API route + queryKey namespace + the 6-case hook test suite per TECH.md §3 P-1, P-2, and P-5.

**Test strategy:** `bun run test -- use-application-types` passes 6 cases per TECH.md §3 P-5 (hook returns 6 rows; useWorkspaceType valid/unknown; useLauncherTypes filter; formatTypeCount singular/plural/undefined-config); `GET /api/application-types` returns 6 rows for an authenticated request; static registry coexists unchanged at this commit (no UI parity regression).

**Details:**

**TECH.md refs:** §3 P-1 (hook signature + cache key + fetcher), §3 P-2 (hook return shape + 3 sync helpers), §3 P-5 (test alignment for the new hook tests), §4 Option C (icon-name → LucideIcon static map + static client config for route/available/hasCustomCreation/features).

**Files to create:**

### 1. `hooks/workspaces/use-application-types.ts` (new)

Mirror the shape of `hooks/intelligence/use-company-profiles.ts` (single-fetch TanStack hook pattern, L33-47). Exports:
- `interface ApplicationTypeRow` — DB row shape (key, label, labelPlural, description, defaultIcon, defaultColour).
- `interface WorkspaceTypeConfig extends ApplicationTypeRow` — adds resolved `icon: LucideIcon`, `route: string | null`, `available: boolean`, `hasCustomCreation: boolean`, `features: { hasStatus, hasContentAssignment, hasDomainMetadata }`.
- `useApplicationTypes()` — `useQuery({ queryKey: queryKeys.applicationTypes.list, queryFn: () => fetchJson<ApplicationTypeRow[]>('/api/application-types'), select: (rows) => rows.map(toWorkspaceTypeConfig), staleTime: 5 * 60_000 })`.
- `useWorkspaceType(type: string)` — wraps `useApplicationTypes()` and `.find(c => c.key === type)`.
- `useLauncherTypes()` — wraps `useApplicationTypes()` and filters per current `getLauncherTypes()` semantics (`route !== null || !available`).
- `formatTypeCount(config: WorkspaceTypeConfig | undefined, n: number)` — pure-function sync utility, no hook.
- Internal static client config keyed by `application_types.key`: `route`, `available`, `hasCustomCreation`, `features.*` for the 3 currently-rendered types (procurement, intelligence, sales_proposal). Plus an icon-name → LucideIcon map for the `defaultIcon` strings (briefcase → Briefcase, newspaper → Newspaper, file-signature → FileSignature; default fallback → Folder).
- Internal `toWorkspaceTypeConfig(row: ApplicationTypeRow): WorkspaceTypeConfig` selector — joins DB row with static client config + resolves Lucide icon. Unknown keys (not in static map) get a permissive default (route=null, available=false, hasCustomCreation=false, features all false, icon=Folder) so the hook never returns an undefined-shape row.

**CLAUDE.md stable-empty-array discipline (R-6):** hoist module-level `const EMPTY_TYPES: WorkspaceTypeConfig[] = [];` and use it as the default when `data` is undefined. Do NOT inline `data ?? []` at consumer call sites — that breaks downstream React memoisation deps.

### 2. `app/api/application-types/route.ts` (new)

Mirror the shape of `app/api/intelligence/profiles/route.ts` GET handler. Single `GET` export:
- `getAuthorisedClient(['admin', 'editor', 'viewer'])` — authenticated, any role (TECH.md Q-3 ratification S251).
- Uses `authFailureResponse(auth)` for failure routing (CLAUDE.md auth pattern gotcha — discriminated union `{ success: boolean }`, never `{ authorised }`).
- `supabase.from('application_types').select('key, label, label_plural, description, default_icon, default_colour').order('label')`.
- Returns `NextResponse.json(data ?? [])`.
- Errors via `safeErrorMessage` per the route precedent.
- Camel-case mapping: the DB returns snake_case (`label_plural`, `default_icon`, `default_colour`); the route can either pass-through snake_case (then the hook's `toWorkspaceTypeConfig` selector maps snake→camel) OR map at the route layer. Recommended: route returns the snake_case shape verbatim from `database.types.ts`; the hook's `select:` selector handles the camel-case normalisation. This keeps the route a thin pass-through.

**NOTE on `ApplicationTypeRow` interface:** TECH.md §3 P-1 declares the interface with camelCase field names (`labelPlural`, `defaultIcon`, `defaultColour`). Decision: the interface is the *post-selector* shape; the route returns snake_case (matching `database.types.ts` Row), and the `select:` selector normalises to camelCase before consumers see it. Document this in the hook module header comment.

### 3. `lib/query/query-keys.ts` (edit — 1 namespace append)

Append to the exported `queryKeys` object (after the `workspaces` namespace at L135-140):

```ts
applicationTypes: {
  all: ['application-types'] as const,
  list: ['application-types', 'list'] as const,
},
```

### 4. `__tests__/hooks/workspaces/use-application-types.test.ts` (new)

Mirror existing TanStack hook test patterns (e.g. `__tests__/hooks/intelligence/use-workspace-flags.test.tsx` for QueryClient setup). Mock strategy: `createMockSupabaseClient()` from `__tests__/helpers/mock-supabase.ts`, returning the 6 seed rows verbatim with the Option C metadata grafted on. Coverage (6 cases per TECH.md §3 P-5):
1. Hook returns 6 rows when `application_types` table is mocked with 6 rows.
2. `useWorkspaceType('procurement')` resolves to a config with `label === 'Procurement'`, `route === '/procurement'`, `available === true`, `hasCustomCreation === true`, `icon === Briefcase` (resolved from string).
3. `useWorkspaceType('unknown_key')` resolves to `undefined` (preserves the current `getWorkspaceType()` contract).
4. `useLauncherTypes()` filters out types where `route === null && available === true` (preserves current `getLauncherTypes()` semantics).
5. `formatTypeCount(undefined, 0)` falls back to `'0 active workspaces'` (preserves current undefined-config branch).
6. `formatTypeCount(intelligenceConfig, 3)` returns `'3 active intelligence streams'` (preserves singular/plural logic).

**Coexistence note:** at this commit the hook + route + queryKey + tests all land but NO UI consumer uses them. The static `WORKSPACE_TYPE_REGISTRY` in `lib/workspace-types.ts` remains fully intact and still serves the 3 UI consumers. This is the intentional intermediate state per TECH.md §5 step 3 — gives the verification gate room to confirm the hook works in isolation before consumer migration.

**Files touched:**
- `hooks/workspaces/use-application-types.ts` (new)
- `app/api/application-types/route.ts` (new)
- `lib/query/query-keys.ts` (1 namespace append)
- `__tests__/hooks/workspaces/use-application-types.test.ts` (new)

**Verification gate after this Subtask:** `bun run test -- use-application-types` passes (6 cases). `bun lint` clean. `/workspaces` launcher still renders identically (no consumer change yet — the static registry still serves).

<info added on 2026-05-21T23:00:00.000Z>
**Wave 1 close — orchestrator promote to done.** Checker verdict on commit `371f07c0` (cherry-picked as `0c370954`) = **PASS_WITH_NOTES**. All 9 acceptance criteria pass. 3 in-scope nits curated to code-simplification pass at Task ID-29 close (per workflow quality-review gate): (1) useLauncherTypes exclusion-branch test coverage gap; (2) vi.stubGlobal vs createMockSupabaseClient mock-strategy clarification — architecturally correct (hook = HTTP boundary), spec amend candidate; (3) `use client` directive at L28 vs convention L1 — cosmetic. Test suite 12,736 pass / 7 fail (baseline carry, zero net-new) / 28 skip / 762 files post-cherry-pick.
</info added on 2026-05-21T23:00:00.000Z>

### ID-29.7: Migrate 3 UI consumers to useApplicationTypes() hook (workspaces-content, workspace-card, workspace-create-dialog)

- **Status:** done
- **Dependencies:** ID-29.6
- **Updated:** _unset_

Replace static `@/lib/workspace-types` imports with `@/hooks/workspaces/use-application-types` hook calls in the 3 UI consumer files per TECH.md §3 P-3a/3b/3c. Each migration preserves rendered behaviour byte-for-byte.

**Test strategy:** All 3 UI consumer files import from `@/hooks/workspaces/use-application-types` (not `@/lib/workspace-types`); `/workspaces` launcher renders identically to pre-refactor (same 3 cards, same counts); workspace-card renders identically (same badge + arrow-up-right); workspace-create-dialog custom-creation `useEffect` still fires when `typeConfig?.hasCustomCreation === true`; `grep -rn '@/lib/workspace-types' app/ components/ hooks/ contexts/` returns zero matches.

**Details:**

**TECH.md ref:** §3 P-3 (UI consumer migration patterns) — explicit before/after blocks for each of the 3 files.

### 1. `app/workspaces/workspaces-content.tsx` (edit; TECH.md §3 P-3a)

**Before (L4, L11, L28):**
```ts
import { getLauncherTypes, formatTypeCount } from '@/lib/workspace-types';
// L11:
const launcherTypes = getLauncherTypes();
// L28:
const countText = formatTypeCount(wt.type, count);
```

**After:**
```ts
import { useLauncherTypes, formatTypeCount } from '@/hooks/workspaces/use-application-types';
// L11:
const { data: launcherTypes = [], isLoading: _isLoading } = useLauncherTypes();
// L28:
const countText = formatTypeCount(wt, count);  // pass the config directly, not the type key
```

**Loading-state handling (TECH.md R-1):** the page is a server component (`app/workspaces/page.tsx`) that fetches counts on the server. The hook adds a render-time async fetch (max 1 round-trip on first paint, served from cache afterwards). On first paint, the existing empty-grid container renders (`launcherTypes = []` returns nothing from `.map()` — the surrounding `<div className="mt-8 grid …">` shows empty until hook resolves ~50ms). Acceptable per AC-3a.

### 2. `components/workspace/workspace-card.tsx` (edit; TECH.md §3 P-3b)

**Before (L18, L46):**
```ts
import { getWorkspaceType } from '@/lib/workspace-types';
// L46:
const typeConfig = workspace.type ? getWorkspaceType(workspace.type) : null;
```

**After:**
```ts
import { useWorkspaceType } from '@/hooks/workspaces/use-application-types';
// L46:
const { data: typeConfig = null } = useWorkspaceType(workspace.type ?? '');
```

The existing null-coalesce + `typeConfig &&` JSX guards at L75 and L84 handle the loading frame (`typeConfig === undefined`) the same way they handle the 'joined-row-absent' case today.

### 3. `components/workspace/workspace-create-dialog.tsx` (edit; TECH.md §3 P-3c)

**Before (L19, L50, L54):**
```ts
import { getWorkspaceType } from '@/lib/workspace-types';
// L50:
const typeConfig = getWorkspaceType(type);
// L54 (in useEffect):
if (open && typeConfig?.hasCustomCreation) { … }
```

**After:**
```ts
import { useWorkspaceType } from '@/hooks/workspaces/use-application-types';
// L50:
const { data: typeConfig } = useWorkspaceType(type);
// L54: useEffect deps unchanged — typeConfig identity changes when hook resolves, triggers effect.
if (open && typeConfig?.hasCustomCreation) { … }
```

**Hook caveat (TECH.md):** dialog opens via controlled `open` prop. On first open it may render once with `typeConfig === undefined` before hook resolves. The existing fallback at L126 (`typeConfig?.label ?? 'Workspace'`) and L128 (`typeConfig?.description ?? 'Create a new workspace.'`) cover this. Custom-creation `useEffect` fires correctly after data resolves (effect deps already include `typeConfig`).

### Post-migration verification (carry into 29.8 Subtask's zero-callers gate)

After all 3 files migrated, `grep -rn '@/lib/workspace-types' app/ components/ hooks/ contexts/` should return ONLY:
- `lib/validation/schemas.ts:3` (the sync constraint import — preserved per TECH.md §1 out-of-scope)
- `__tests__/lib/workspace-types.test.ts:14` (test file, to be pruned in 29.8)

Zero imports in `app/`, `components/`, `hooks/`, `contexts/`.

**No raw Tailwind colours discipline (CLAUDE.md):** none of these 3 files introduce new colour classes; they only swap imports and hook calls. No semantic-token work required.

**Files touched:**
- `app/workspaces/workspaces-content.tsx` (~5-line diff)
- `components/workspace/workspace-card.tsx` (~5-line diff)
- `components/workspace/workspace-create-dialog.tsx` (~5-line diff)

**Verification gate after this Subtask:** `/workspaces` launcher renders identically (counts + cards). Workspace card renders identically on browse page (type badge + label + arrow-up-right indicator). Workspace create dialog opens identically; if `type='procurement'` (default), custom-creation flow triggers as before. `bun run test` clean. `bun lint` clean. `bun run build` clean.

<info added on 2026-05-21T22:52:03.128Z>
**Shipped (this commit):** ID-29.7 — migrated 3 UI consumers from static `@/lib/workspace-types` to `useApplicationTypes` TanStack hook. Source files: `app/workspaces/workspaces-content.tsx` (getLauncherTypes→useLauncherTypes; `wt.type`→`wt.key` for counts/key/aria; formatTypeCount(wt,n) now passes resolved config object), `components/workspace/workspace-card.tsx` (getWorkspaceType→useWorkspaceType destructure-default-null), `components/workspace/workspace-create-dialog.tsx` (getWorkspaceType→useWorkspaceType; useEffect deps unchanged — typeConfig identity changes when hook resolves, triggers effect for custom-creation delegation).

**Acceptance vs testStrategy:**
- ✓ All 3 UI consumer files import from `@/hooks/workspaces/use-application-types` (zero references to `@/lib/workspace-types` in app/components/hooks/contexts).
- ✓ `grep -rn '@/lib/workspace-types' app/ components/ hooks/ contexts/` returns ZERO matches (verified post-migration).
- ✓ Sync constraint import preserved at `lib/validation/schemas.ts:3` (per TECH.md §1 out-of-scope).
- ✓ `__tests__/lib/workspace-types.test.ts:14` import preserved (pruning is ID-29.8 scope).
- ✓ All 3 component tests still pass (25/25); broader workspace-related regression 81/81.
- ✓ workspace-create-dialog custom-creation `useEffect` still fires when `typeConfig?.hasCustomCreation === true` (verified via `procurement` and `sales_proposal` test cases — procurement fires the delegate, sales_proposal does not).

**Deviations from brief (in-scope necessities for `testStrategy` compliance):**
1. **Test-file wrap with `QueryClientProvider` + `fetch` stub** — the 3 component tests (`__tests__/components/workspace-card.test.tsx`, `__tests__/components/workspace-create-dialog.test.tsx`, `__tests__/app/workspaces/workspaces-content.test.tsx`) and the closely-coupled `__tests__/app/workspaces/workspaces-launcher.test.tsx` previously relied on the static module-init registry resolving synchronously. Once the consumers use `useQuery`, jsdom renders throw `No QueryClient set, use QueryClientProvider to set one`. Added a local `createWrapper()` helper + `beforeEach`/`afterEach` `vi.stubGlobal('fetch', ...)` returning the 6 application_types seed rows verbatim (snake_case) — mirroring the pattern in `__tests__/hooks/workspaces/use-application-types.test.ts`. This was the only way to satisfy the brief's acceptance line `bun run test clean (component tests still pass)`.
2. **Test fixture key swap `proposal` → `sales_proposal`** — pre-migration, the static registry exposed `proposal` (label "Sales Proposal"); the DB seed uses `sales_proposal`. Three pre-existing test cases referenced `type: 'proposal'` (workspace-card "does not show arrow icon for types without a route"; 3× workspace-create-dialog "sales_proposal type — no custom creation"). Updated those fixtures to `type: 'sales_proposal'` so the hook resolves to the DB row. Visual rendered output is identical (label "Sales Proposal", description matches DB backfill, route null, available false, hasCustomCreation false from CLIENT_CONFIG).
3. **Field rename `wt.type` → `wt.key` in `workspaces-content.tsx` (3 sites)** — the hook's `WorkspaceTypeConfig` uses `key` (matching DB column), not `type` (the pre-refactor field name). The brief's "After" block at L28 — `formatTypeCount(wt, count)` — only works against `WorkspaceTypeConfig` shape so this rename was implied. Sites updated: counts lookup (L27), React key on `<Link>` (L33), React key on coming-soon `<div>` (L60).

**Out-of-scope observations (for Curator triage):**
- **Pre-existing TypeScript error in `supabase/types/database.types.ts:4539`** (multi-line `TS1434`/`TS1005`/`TS1228` errors). Verified pre-existing via `git stash && bunx tsc --noEmit` on bare track branch. NOT introduced by this Subtask; my migrated files have zero TS errors. Likely a generated-types staleness or a regen artifact from a prior unrelated commit. Should be triaged to a regen-types Subtask.
- **`__tests__/lib/date-extraction-integration.test.ts` vi.hoisted/vi.mock warnings** — emitted by `bun run test --changed`. Pre-existing, unrelated to this Subtask.
- **Static registry deletion is ID-29.8 scope** (per TECH.md §5 step 5 + brief). The static `WORKSPACE_TYPE_REGISTRY`, inline `registerType({…})` calls, `getWorkspaceType()`, `getAllWorkspaceTypes()`, `getLauncherTypes()`, `formatTypeCount()`, and the `WorkspaceTypeConfig` interface in `lib/workspace-types.ts` are now orphaned UI metadata — only `APPLICATION_TYPE_KEYS` and `getValidTypeValues()` are still consumed (by `lib/validation/schemas.ts:3`). Ready for the next Subtask's pruning pass.

**Verification commands run:**
- `bun run test __tests__/components/workspace-card.test.tsx __tests__/components/workspace-create-dialog.test.tsx __tests__/app/workspaces/workspaces-content.test.tsx __tests__/app/workspaces/workspaces-launcher.test.tsx` → 41/41 PASS.
- `bun run test __tests__/components/workspace-card.test.tsx __tests__/components/workspace-create-dialog.test.tsx __tests__/components/workspace-selector.test.tsx __tests__/app/workspaces __tests__/app/workspaces-page.test.tsx __tests__/app/workspaces-boundaries.test.tsx __tests__/hooks/workspaces __tests__/lib/workspace-types.test.ts` → 81/81 PASS (broader workspace regression).
- `bun lint` → clean (zero errors, zero warnings).
- `bunx tsc --noEmit` → zero new errors in changed files (pre-existing database.types.ts errors noted out-of-scope above).
- `grep -rn '@/lib/workspace-types' app/ components/ hooks/ contexts/` → zero matches.
- `bun run test --changed` → 5 test files / 38 tests PASS.
</info added on 2026-05-21T22:52:03.128Z>


### ID-29.8: Delete static WORKSPACE_TYPE_REGISTRY surface from lib/workspace-types.ts + align legacy tests

- **Status:** done
- **Dependencies:** ID-29.7
- **Updated:** _unset_

Delete the static registry surface from `lib/workspace-types.ts` (`WORKSPACE_TYPE_REGISTRY`, `registerType()`, the 3 inline `registerType({…})` calls, `getWorkspaceType()`, `getAllWorkspaceTypes()`, `getLauncherTypes()`, `formatTypeCount()`, `WorkspaceTypeConfig`, `logger` + Lucide imports). Prune `__tests__/lib/workspace-types.test.ts` to retain only `getValidTypeValues` coverage. `APPLICATION_TYPE_KEYS` tuple + `getValidTypeValues()` preserved verbatim.

**Test strategy:** Zero-callers gate cleared (`grep -rn` returns zero matches for each deleted symbol outside `lib/workspace-types.ts` itself); `lib/workspace-types.ts` retains ONLY `APPLICATION_TYPE_KEYS` + `getValidTypeValues()` + updated header comment; `__tests__/lib/workspace-types.test.ts` retains only the `describe('getValidTypeValues')` block; `bun run test` + `bun lint` + `bun run build` + `bun run knip` all clean; only surviving `@/lib/workspace-types` import in production code is `lib/validation/schemas.ts:3`.

**Details:**

**TECH.md refs:** §3 P-4 (static registry deletion), §3 P-5 (test alignment for the legacy test file).

### Zero-callers verification gate (run BEFORE any delete)

For each symbol about to be deleted, confirm zero remaining call sites via `grep -rn` across `app/`, `components/`, `lib/`, `hooks/`, `contexts/`, `__tests__/`. Optional: `bun scripts/ast-dataflow-cli.ts callers <symbol>` for type-checker-resolved verification.

Expected: every deleted symbol returns ZERO matches outside `lib/workspace-types.ts` itself. If any match exists, halt and surface — the migration in 29.7 missed a site (this should not occur given 29.7's full inventory was confirmed pre-decomposition).

The ONLY surviving `@/lib/workspace-types` import in the codebase should be `lib/validation/schemas.ts:3` (`import { getValidTypeValues } from '@/lib/workspace-types'`).

### Deletions from `lib/workspace-types.ts`

Per TECH.md §3 P-4 deletion table:

| Symbol                              | Lines (current) | Deletion gate                              |
| ----------------------------------- | --------------- | ------------------------------------------ |
| `WorkspaceTypeConfig` interface     | L18-61          | Moved into `use-application-types.ts` in 29.6 |
| `WORKSPACE_TYPE_REGISTRY` const     | L64             | Zero remaining importers                   |
| `registerType()`                    | L67-73          | Zero remaining callers (all 3 inline)      |
| Inline `registerType({…})` ×3       | L81-136         | Zero remaining callers                     |
| `getWorkspaceType()`                | L141-145        | Replaced by `useWorkspaceType()` hook      |
| `getAllWorkspaceTypes()`            | L148-150        | Subsumed by `useApplicationTypes().data`   |
| `getLauncherTypes()`                | L153-157        | Replaced by `useLauncherTypes()` hook      |
| `formatTypeCount()`                 | L186-192        | Re-exported from `use-application-types.ts` in 29.6 |
| `logger` import                     | L3              | Deleted with `registerType()`              |
| Lucide icon imports + `LucideIcon`  | L1-2            | Moved into icon-name map in hook module in 29.6 |

### Retained verbatim in `lib/workspace-types.ts`

- `APPLICATION_TYPE_KEYS` tuple at L166-173 — DO NOT TOUCH.
- `getValidTypeValues()` function at L181-183 — DO NOT TOUCH.
- Top-of-file comment block (L1-11) — UPDATE to reflect Path c completion. Replace the existing comment with a header stating: 'Post-ID-29 (S252+): sync source of truth for `application_types.key` values, paired with the DB seed in `application_types`. Used by `lib/validation/schemas.ts` to construct Zod enum at module-load. The UI metadata surface (label, labelPlural, description, icon, route, available, hasCustomCreation, features, defaultColour, defaultIcon) lives in `hooks/workspaces/use-application-types.ts` as a TanStack Query hook over `application_types` DB rows joined with a static client config (Option C hybrid per docs/specs/tanstack-workspace-types/TECH.md). Add or retire a seed key: update both `APPLICATION_TYPE_KEYS` AND `application_types` table seed in lockstep.'

Post-deletion, `lib/workspace-types.ts` is ~25 lines.

### Prune `__tests__/lib/workspace-types.test.ts` (TECH.md §3 P-5)

Delete the following `describe()` blocks:
- `describe('getWorkspaceType')`
- `describe('getAllWorkspaceTypes')`
- `describe('getLauncherTypes')`
- `describe('formatTypeCount')`

Retain ONLY:
- `describe('getValidTypeValues')` block (tuple shape + 6-key invariant)

Update top-of-file imports: remove `getWorkspaceType, getAllWorkspaceTypes, getLauncherTypes, formatTypeCount` from the import statement at L7-14; retain only `getValidTypeValues`.

The new hook-test coverage lives in `__tests__/hooks/workspaces/use-application-types.test.ts` (authored in 29.6).

### Final regression sweep

After deletes + test prune:
1. `bun run test` — full suite passes.
2. `bun lint` — clean.
3. `bun run build` — clean (no TS errors from missing exports).
4. `bun run knip` — no new unused-export findings (the 5 deleted symbols + `logger` import + Lucide imports should fully drop out).
5. `grep -rn '@/lib/workspace-types' .` — returns ONLY `lib/validation/schemas.ts:3` and `__tests__/lib/workspace-types.test.ts:` (test file — narrowed to `getValidTypeValues` import).
6. Manual smoke: `/workspaces` launcher renders identically; workspace-card renders identically; workspace-create-dialog opens identically.

**Files touched:**
- `lib/workspace-types.ts` (delete ~168 lines, retain ~25 lines, update header comment)
- `__tests__/lib/workspace-types.test.ts` (delete ~4 describe blocks, retain `getValidTypeValues` coverage)

**Verification gate after this Subtask:** end-to-end Task ID-29 complete. Static registry deleted; hook in production; UI parity preserved; sync constraint preserved; all tests pass; knip clean.

<info added on 2026-05-21T23:22:00.000Z>
**Shipped:** ID-29.8 — deleted `WORKSPACE_TYPE_REGISTRY` + 4 functions + `WorkspaceTypeConfig` + 3 inline `registerType()` calls + `logger` + Lucide imports from `lib/workspace-types.ts`; pruned `__tests__/lib/workspace-types.test.ts` to retain only `describe('getValidTypeValues')`. End-to-end Task ID-29 implementation complete.
**Commit:** (worktree branch `worktree-agent-af2cbe64933470534` — SHA via `git log -1` post-commit)
**Files touched:** `lib/workspace-types.ts` (193 → 37 lines, -156), `__tests__/lib/workspace-types.test.ts` (154 → 47 lines, -107), `docs/reference/task-list.json` (status `pending → in-progress` + this journal block).
**Zero-callers gate:** CLEARED — all 7 symbols (`getWorkspaceType`, `getAllWorkspaceTypes`, `getLauncherTypes`, `formatTypeCount`, `WORKSPACE_TYPE_REGISTRY`, `WorkspaceTypeConfig`, `registerType`) have ZERO remaining callers outside `lib/workspace-types.ts` itself. The only `@/lib/workspace-types` imports repo-wide are `lib/validation/schemas.ts:3` (`getValidTypeValues`) + `__tests__/lib/workspace-types.test.ts:12` (`getValidTypeValues`).
**Post-deletion `lib/workspace-types.ts`:** 37 lines (12-line updated header comment + 25 lines code: `APPLICATION_TYPE_KEYS` tuple + `getValidTypeValues()`).
**Acceptance (testStrategy mapping):**
- Zero-callers gate cleared: PASS.
- `lib/workspace-types.ts` retains only target surface: PASS (37 lines).
- `__tests__/lib/workspace-types.test.ts` retains only `describe('getValidTypeValues')`: PASS (one describe block, 4 tests, 4/4 pass).
- `bun run test` (scoped pruned file): 4/4 PASS.
- `bun run test` (full suite): 7 failures, **ALL pre-existing on track HEAD `a305aa85`** (proven by stash-and-rerun) — mammoth fixture missing (5), markdown-parity (1), doc-freshness untracked taxonomy runbook (1). None caused by ID-29.8.
- `bun lint`: CLEAN.
- `bun run build`: pre-existing `.bin` ENOENT failure on track HEAD (worktree environmental, not caused by ID-29.8 — proven by stash-and-rerun).
- `bun run knip`: only diff vs baseline is line-number shift of pre-existing `APPLICATION_TYPE_KEYS` finding (L166 → L20). ZERO new orphans introduced.
**Deviations from brief:**
- Test-file `describe('getValidTypeValues')` block: dropped the original `'excludes unavailable types'` test (asserted `expect(values).not.toContain('proposal')`) because it was stale — the tuple never contained `'proposal'` (only `'sales_proposal'`), and the assertion contradicted the post-T2 6-key state. Added a stronger `'returns all 6 application_types seed keys'` assertion (`expect(values).toEqual([6 explicit keys])`) consistent with the file's post-deletion purpose as the sync source of truth. The other 3 retained tests are verbatim. Net effect: tighter sync-constraint coverage.
**Out-of-scope observations:**
- Pre-existing test/build failures (mammoth, markdown-parity, doc-freshness, `.bin` ENOENT) all surfaced as track-baseline issues. Curator candidate for triage.
- `taxonomy-change-runbook.md` exists in worktree at 22 May 00:11 but doc-freshness test still fails — appears the file was added AFTER track HEAD `a305aa85` (created 22 May 00:09) but doc-freshness fixtures weren't refreshed in lockstep. Curator candidate.
- `APPLICATION_TYPE_KEYS` flagged by knip as unused export (pre-existing baseline finding, not new). The tuple is consumed internally by `getValidTypeValues()` but the export is currently unconsumed externally. Could downgrade to non-export if not needed elsewhere — Curator candidate.
</info added on 2026-05-21T23:22:00.000Z>
