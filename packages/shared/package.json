{
  "name": "@task-view/shared",
  "version": "0.1.0-task-view-prep",
  "private": true,
  "description": "task-view shared helpers — kept after the §1.2 strip: repo info + draft persistence + code-path utilities + ledger config + Bun code-nav adapter. All annotation / agent-jobs / external-annotation / share-url / VCS / PR / tour / prompts modules removed.",
  "exports": {
    "./code-file": "./code-file.ts",
    "./code-nav": "./code-nav.ts",
    "./config": "./config.ts",
    "./draft": "./draft.ts",
    "./extract-code-paths": "./extract-code-paths.ts",
    "./repo": "./repo.ts",
    "./subtask-progress": "./subtask-progress.ts"
  },
  "dependencies": {}
}
